# MAIL MONSTER PRO V3.0 - 의존성
# Python 3.10+ 권장 (3.12 권장, 3.14에서 pywebview 빌드 이슈 있을 수 있음)

customtkinter>=5.2.0
Pillow>=10.0.0
pystray>=0.19.0
pandas>=2.0.0
openpyxl>=3.1.0
gspread>=5.12.0
google-auth>=2.23.0
requests>=2.28.0

# WYSIWYG 에디터용 (선택). Windows에서 pip 설치 실패 시 미설치해도 앱 실행 가능(에디터만 비활성).
# pywebview>=4.4.0
